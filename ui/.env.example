# API для запросов (через Vite proxy в dev можно не указывать)
# VITE_API_URL=/api

# Прямой URL бэкенда для auth/OAuth (должен совпадать с Callback URL в Яндексе)
# VITE_AUTH_URL=http://localhost:3000
